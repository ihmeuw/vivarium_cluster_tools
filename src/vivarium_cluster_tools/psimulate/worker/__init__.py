"""
====================
psimulate Workers
====================

Worker implementations for psimulate tasks.

The ``task_runner`` module provides the CLI entry point for individual Jobmon
tasks. Each task loads a job spec JSON file, runs the appropriate work horse,
and writes results to the staging directory.

"""
